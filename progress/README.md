# Progress

Persistent learnings between coding agent attempts. Each attempt reads this directory before starting, writes findings here after finishing (success or failure).

Files are named by attempt: `attempt-001.md`, `attempt-002.md`, etc.

This is the agent's "memory between runs" — what worked, what didn't, what to try differently.
